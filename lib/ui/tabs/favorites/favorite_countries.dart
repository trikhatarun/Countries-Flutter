import 'package:flutter/material.dart';

class FavoriteCountries extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    print("rebuilt favorites");
    return Container(
      color: Colors.blue,
    );
  }
}